<h1>Currency pairs</h1>

Go <a href = "/">back</a>

<ul>
    <% @array_of_symbols.each do |a_symbol| %>
    <li>
      <a href = "/forex/<%= a_symbol %>">Convert <%= a_symbol %> to...</a>
    </li>
  <% end %>
</ul>
